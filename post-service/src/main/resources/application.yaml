eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 9010
  servlet:
    context-path: /posts

spring:
  application:
    name: post-service

  datasource:
    url: jdbc:postgresql://localhost:5432/social_postDB
    username: postgres
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.LongSerializer
      value-serializer: org.springframework.kafka.support.serializer.JacksonJsonSerializer
      properties:
        spring.json:
          trusted.packages: com.example.linkedInProject.*



---

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false


spring:
  config:
    activate:
      on-profile: k8s

  datasource:
    url: jdbc:postgresql://${DB_SERVER}:5432/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  kafka:
    bootstrap-servers: kafka:9092

CONNECTION_SERVICE_URI: http://connection-service
UPLOADER_SERVICE_URI: http://uploader-service